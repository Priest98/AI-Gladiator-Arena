# Dependency directories
node_modules/

# Build outputs
client/dist/

# Local database file
server/database.json

# Environment configuration files (secrets)
.env
.env.local
.env*.local
server/.env
server/.env.local
*.log

# Allow client production env (contains only public VITE_ vars, no secrets)
!client/.env.production

.vercel
